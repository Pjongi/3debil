#version 330 core

// Ten shader jest pusty, ponieważ potrzebujemy tylko zapisu głębi,
// co dzieje się automatycznie. Można dodać 'discard;' jeśli FBO ma tylko depth attachment.
// void main() {} // Może być całkowicie pusty